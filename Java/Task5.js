'use strict';

var number1 = 1
var number2 = 2

function computation (number1, number2) {
    return console.log(number1 + number2, number1 - number2, number1 * number2, number1 / number2,)
}

computation(number1, number2)